<?php

/**
 * The custom pages switch and page types moved to Settings. Kept so a `pages`
 * resource `.meta` that still names it as `admin_panel` renders nothing.
 */
function managed_pages_toggle_sc($params)
{
    return '';
}
